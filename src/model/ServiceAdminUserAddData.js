/**
 * White Label Communications CPaas API Documentation
 * A CPaaS platform API
 *
 * The version of the OpenAPI document: 1.1
 * Contact: support@whitelabelcomm.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The ServiceAdminUserAddData model module.
 * @module model/ServiceAdminUserAddData
 * @version 1.1
 */
class ServiceAdminUserAddData {
    /**
     * Constructs a new <code>ServiceAdminUserAddData</code>.
     * @alias module:model/ServiceAdminUserAddData
     * @param email {String} 
     * @param role {module:model/ServiceAdminUserAddData.RoleEnum} 
     */
    constructor(email, role) { 
        
        ServiceAdminUserAddData.initialize(this, email, role);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, email, role) { 
        obj['email'] = email;
        obj['role'] = role;
    }

    /**
     * Constructs a <code>ServiceAdminUserAddData</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/ServiceAdminUserAddData} obj Optional instance to populate.
     * @return {module:model/ServiceAdminUserAddData} The populated <code>ServiceAdminUserAddData</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new ServiceAdminUserAddData();

            if (data.hasOwnProperty('email')) {
                obj['email'] = ApiClient.convertToType(data['email'], 'String');
            }
            if (data.hasOwnProperty('role')) {
                obj['role'] = ApiClient.convertToType(data['role'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>ServiceAdminUserAddData</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>ServiceAdminUserAddData</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of ServiceAdminUserAddData.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['email'] && !(typeof data['email'] === 'string' || data['email'] instanceof String)) {
            throw new Error("Expected the field `email` to be a primitive type in the JSON string but got " + data['email']);
        }
        // ensure the json data is a string
        if (data['role'] && !(typeof data['role'] === 'string' || data['role'] instanceof String)) {
            throw new Error("Expected the field `role` to be a primitive type in the JSON string but got " + data['role']);
        }

        return true;
    }


}

ServiceAdminUserAddData.RequiredProperties = ["email", "role"];

/**
 * @member {String} email
 */
ServiceAdminUserAddData.prototype['email'] = undefined;

/**
 * @member {module:model/ServiceAdminUserAddData.RoleEnum} role
 */
ServiceAdminUserAddData.prototype['role'] = undefined;





/**
 * Allowed values for the <code>role</code> property.
 * @enum {String}
 * @readonly
 */
ServiceAdminUserAddData['RoleEnum'] = {

    /**
     * value: "admin"
     * @const
     */
    "admin": "admin",

    /**
     * value: "dev"
     * @const
     */
    "dev": "dev"
};



export default ServiceAdminUserAddData;

